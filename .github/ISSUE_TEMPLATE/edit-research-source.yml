name: Edit a research source
description: Change a research source query, feed metadata, category, tags, weight, or status.
title: "[Research Edit] "
body:
  - type: input
    id: source-id
    attributes:
      label: Research source ID
    validations:
      required: true
  - type: input
    id: display-name
    attributes:
      label: Display name
      description: Optional. Leave empty to preserve the value.
  - type: input
    id: arxiv-query
    attributes:
      label: arXiv query
      description: Optional and valid only for arxiv_query sources.
  - type: input
    id: feed-url
    attributes:
      label: Public HTTPS feed URL
      description: Optional and valid only for rss_feed sources.
  - type: input
    id: publisher
    attributes:
      label: Publisher
  - type: input
    id: category
    attributes:
      label: Category
  - type: input
    id: tags
    attributes:
      label: Tags
      description: Optional complete comma-separated list.
  - type: dropdown
    id: weight
    attributes:
      label: Source weight
      options: [unchanged, "1", "2", "3", "4", "5"]
    validations:
      required: true
  - type: dropdown
    id: status
    attributes:
      label: Status
      options: [unchanged, enabled, disabled]
    validations:
      required: true
  - type: textarea
    id: reason
    attributes:
      label: Why is this change needed?
    validations:
      required: true
