# Scripts

Command-line entry points for registry management, collection, monitoring, validation, and dashboard generation will be introduced in later phases.

Scripts should delegate business logic to workspace packages so they remain thin orchestration layers.
